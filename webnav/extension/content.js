// WebNav Content Script
// Injected into all pages for DOM access
// Most functionality is in background.js using chrome.scripting.executeScript

// This file exists for potential future use cases where we need
// persistent page context (e.g., mutation observers, event listeners)

console.log("[WebNav] Content script loaded");
