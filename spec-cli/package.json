{
  "name": "spec-cli",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "spec": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun run build.ts",
    "build:all": "bun run build.ts --all-platforms"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
